(Optimization||done with||SGD)
(Optimization||done with||optionally)
(SGD||with||mini-batch size 50 or 100)
(SGD||with||optionally)
(mini-batch size 50 or 100||with||momentum)
(rmsprop||for||acceleration)
(optionally||has||rmsprop)
(our one - hot bidirectional LSTM with pooling ( oh - 2 LSTMp )||outperforms||word - vector LSTM ( wv - LSTM ))
(word - vector LSTM ( wv - LSTM )||on||all the datasets)
(Contribution||has||Experiments)
