(Experiments||has||Experiments ( supervised ))
(Optimization||done with||SGD)
(Optimization||done with||optionally rmsprop)
(SGD||with||mini-batch size 50 or 100)
(SGD||with||optionally rmsprop)
(mini-batch size 50 or 100||with||momentum)
(optionally rmsprop||for||acceleration)
(one - hot bidirectional LSTM||with||pooling)
(one - hot bidirectional LSTM||outperforms||word - vector LSTM ( wv - LSTM ))
(word - vector LSTM ( wv - LSTM )||on||all the datasets)
(pooling||name||word - vector LSTM ( wv - LSTM ))
(Contribution||has||Experiments)
